<h1> New message </h1>

<%= form_with(model: [@message.chat, @message]) do |f|%>
  <div class = "field">
    <%= f.text_field :content%>
    <%= f.submit "send" %>
  </div>
<% end %>

<%= link_to 'back', @message.chat %>
   