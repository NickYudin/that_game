<%= button_to [:fight], class: "room_btn btn-danger rounded shadow btn-lg" do%>
  Attack!
<% end %> Your weopon: <strong><%=Weapon.find(character.items.active_weapon.first.weapon_id).name %></strong>
