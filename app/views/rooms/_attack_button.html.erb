<%= button_to [:fight], class: "room_btn btn-danger rounded shadow btn-lg" do%>
  Attack!
<% end %>

<%= form_with url: use_weapon_path, method: :post do |f| %>  
    <%= f.select :weapon_id,  character.weapons.map {|weapon| [weapon.name, weapon.id]}, {} %>
    <%= f.submit%>
<% end %>
