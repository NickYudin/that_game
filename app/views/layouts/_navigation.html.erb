<nav class="navbar navbar-expand-lg shadow navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">A Game</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "monsters", monsters_path, class:"nav-link active" %>
        </li>

        <li class="nav-item">
          <% if current_user && character_exist? %>
            <%= link_to "jorney", room_path(current_user.last_room), class:"nav-link active" %>
          <% else %>
            <%= link_to "jorney", new_user_session_path, class:"nav-link active" %>
          <% end %>
        </li>
    </div>
  </div>
</nav>