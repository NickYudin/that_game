<%= turbo_frame_tag "name" do%>

  <%= character.name %>
  
  <% if can? :manage, character%>
    <%= link_to "Change name", character_edit_name_path(character), character: character, class:"btn-sm btn-warning" %>
  <% end %>
  
<% end %>